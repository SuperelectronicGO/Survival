using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SetAsPreview : MonoBehaviour
{
    /*
    [SerializeField] private BuildingManager manager;
    // Start is called before the first frame update
    void Start()
    {
        manager = GameObject.Find("Canvas/Inventory").GetComponent<BuildingManager>();
        manager.buildingPreview = this.gameObject;

    }
    
    */

}
